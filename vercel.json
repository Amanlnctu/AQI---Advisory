{
  "buildCommand": "cd \"frontend website\" && npm install && npm run build",
  "outputDirectory": "frontend website/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
